function test(){
  
}

// @TODO:
// 1) Change restoreMonth() so it places the month based on either:
//   A) Dec = do nothing, already in last position
//   B) ref the months that are already stored and move to the right of correct month, i.e. Mar being stored? Search for Feb's index then move to right of that
